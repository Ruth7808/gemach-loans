
# גמ"ח כספים — מערכת ניהול

## מה זה
מערכת לניהול גמ"ח כספים קהילתי. משתמש יחיד (בעל הגמ"ח, לא איש טכני).
המערכת רק רושמת ומזכירה — לא נוגעת בכסף ולא מתחברת לבנק.

## סטאק
- Frontend: React + TypeScript
- Backend: Node + TypeScript
- DB: SQLite דרך Prisma
- הכל רץ מקומית על השרת של בעל הגמ"ח. פרטיות היא דרישה.

## הממשק
- עברית מלאה, RTL מלא
- פשוט מאוד — המשתמש לא טכני

## מבנה התיקיות
- server/ — Node + TypeScript + Prisma + ה-DB (רק השרת ניגש ל-DB)
- client/ — React + TypeScript
- ה-DB הוא SQLite ב-server/prisma/dev.db

## מודל הנתונים
מקור האמת המלא הוא prisma/schema.prisma — תמיד לקרוא משם, לא לנחש.
עקרונות שלא נראים בסכימה:
- לוח תשלומים נשמר מפורש ב-installments (לא מחושב on-the-fly)
- תשלום חלקי/מוקדם/מרובה-חודשים: מוקצה FIFO מהישן לחדש, דרך allocations
- הלוואה "באיחור": עבר due_date וגם paid < amount. 0 ימי חסד כברירת מחדל.
- כסף פנוי = יתרת פתיחה (settings) + סך תשלומים שנכנסו + סך הפקדות − סך קרן
  שהולוותה − סך משיכות (משיכות עוד לא קיימות בפועל — יתווספו בשלב ב׳ של מפקידים)

## מחוץ להיקף — אל תציע להוסיף
ערבים, צ'קים דחויים, בוט וואטסאפ, תזכורות אוטומטיות, n8n,
אינטגרציות Google, דוחות, ניהול משתמשים מרובים.

מפקידים/הפקדות/בקשות משיכה נוספו לתחום הפרויקט (ראו PLAN/12 ואילך) — לא
מחוץ להיקף יותר.

## מסכים (מבנה, לא עיצוב)
1. דשבורד — מי אמור לשלם היום, מי באיחור, כמה כסף פנוי, כמה אמור לחזור החודש
2. רשימת לווים
3. כרטיס לווה — פרטים, ההלוואות שלו, היסטוריית תשלומים, יתרה
4. רשימת הלוואות — פעילות / באיחור / נסגרו
5. כרטיס הלוואה — סכום, לוח תשלומים, מה שולם, מה נותר, רישום תשלום חדש
6. רשימת מפקידים
7. כרטיס מפקיד — פרטים, ההפקדות שלו, סה״כ הפקדות, רישום הפקדה חדשה
   (בקשות משיכה יתווספו לכרטיס בשלב ב׳ — לא קיימות עדיין)

ניווט: מרשימת לווים → כרטיס לווה → כרטיס הלוואה. מרשימת מפקידים → כרטיס מפקיד.
עיצוב לא נקבע כאן — מחליטים מול המסך תוך כדי בנייה.

## סדר בנייה
1. סכימת Prisma + מיגרציה
2. CRUD לווים
3. יצירת הלוואה + generation אוטומטי של לוח תשלומים
4. רישום תשלום + מנגנון הקצאה FIFO
5. כרטיס הלוואה מלא
6. כרטיס לווה
7. דשבורד (אחרון)

## עקרון עבודה
היקף קטן. כל שלב מסתיים במשהו שרץ ואפשר לראות. לא מתקדמים לשלב הבא
לפני שהנוכחי עובד. אם המשתמשת מנסה להוסיף יכולת לפני שהבסיס עובד — עצור אותה.

## שיטת העבודה שלנו
בכל שלב חדש עובדים כך:
1. אני אומרת מה אני רוצה בשלב הזה
2. אתה כותב תוכנית לקובץ PLAN/<מספר>-<שם-השלב>.md — מה בדיוק תעשה, אילו קבצים, אילו endpoints
   בסוף כל קובץ תוכנית מוסיפים תמיד:

   ## instructions

   1. try to be simple as possible
   2. ask me questions, never assume
   3. grill me
3. אני עוברת על התוכנית ומאשרת לפני שאתה כותב קוד
4. רק אחרי אישור — אתה מבצע
5. כשעובד, commit עם הודעה ברורה בעברית

אל תדלג על שלב האישור. אל תעשה commit לפני שנאמר שזה עובד.